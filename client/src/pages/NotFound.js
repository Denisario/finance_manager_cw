import {Container, Row} from "react-bootstrap";

const NotFound = (props)=>{
    return <Container>
        <Row>
            <div>Page not found</div>
            <div>Log in or register to open this page</div>
        </Row>
    </Container>
}

export default NotFound;